# speed-sc-189
四川电信宽带测速工具

## bash 脚本
### 安装与运行

1. **下载脚本**

   ```bash
   curl -O [https://raw.githubusercontent.com/foxxorcat/speed-sc-189/main/speed_sc_189.sh](https://raw.githubusercontent.com/foxxorcat/speed-sc-189/main/speed_sc_189.sh)
   # 或者
   wget [https://raw.githubusercontent.com/foxxorcat/speed-sc-189/main/speed_sc_189.sh](https://raw.githubusercontent.com/foxxorcat/speed-sc-189/main/speed_sc_189.sh)
   ```

2. **赋予执行权限**

   ```bash
   chmod +x speed_test_189.sh
   ```

3. **开始测速**

   ```bash
   ./speed_test_189.sh
   ```

## 📖 使用指南

### 基础用法

```bash
./speed_test_189.sh [命令] [选项]
```

**可用命令：**

* `test` : 开始测速（默认命令）
* `info` : 仅显示宽带签约信息和当前 IP
* `nodes`: 列出所有可用测速节点及延迟

### 选项参数

| 选项 | 说明 | 默认值 |
| ----- | ----- | ----- |
| `--mode` | 测速模式，可选 `all` (全部), `down` (仅下载), `up` (仅上传) | `all` |
| `--duration` | 测速时长（秒） | `10` |
| `--threads` | 并发线程数 | `8` |
| `--ip` | 强制协议栈，可选 `auto`, `ipv4`, `ipv6` | `auto` |
| `--select` | 进入交互式节点选择模式 (仅 `test` 命令有效) | 关闭 |

### 常见示例

**1. 默认测速**

```bash
./speed_test_189.sh
```

**2. 使用 IPv6 测速**

```bash
./speed_test_189.sh --ip ipv6
```

**3. 高并发测试（适用于千兆/万兆环境）**
使用 16 线程，测速时长延长至 20 秒：

```bash
./speed_test_189.sh --threads 16 --duration 20
```

**4. 仅查看宽带信息**

```bash
./speed_test_189.sh info
```

**5. 手动选择测速节点**

```bash
./speed_test_189.sh test --select
```

## ⚠️ 免责声明

本项目仅供学习交流使用，测速结果仅供参考。脚本通过调用 189 官方公开接口实现，不保证接口的长期可用性。请勿用于商业用途或对服务器进行恶意攻击。

## 📄 开源协议

MIT License
